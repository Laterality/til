# 2026-04-10

## OpenTelemtry

### 개념

#### 신호

##### 로그

###### 구조화, 비구조화, 반구조화(semistructured)된 로그

**반구조화된 로그**

반구조화된 로그는 기계가 읽을 수 있는 키/값 쌍이나 구분된 필드를 갖지만 출처에 따라 안정적인 스키마를 보장하진 않는다. 예시는 아래의 키=값 로깅 혹은 필드 이름과 타입이 메시지마다 다른 JSON 블롭이 있다. 반구조화된 로그는 대체로 비구조화된 로그보다 파싱하기 쉽지만 분석 전에 처리와 정규화가 여전히 필요할 수 있다.

반구조화된 로그의 예:

```
2024-08-04T12:45:23Z level=ERROR service=user-authentication userId=12345 action=login message="Failed login attempt" error="Invalid password" ipAddress=192.168.1.1 userAgent="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/104.0.0.0 Safari/537.36"
```

반구조화된 로그는 다운스트림 분석에서 용이하게 하기 위해 인제스트 과정에서 매핑이나 타입 강제가 필요할 수 있다.
